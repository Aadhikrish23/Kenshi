database:
  address:
  - "${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

socket:
  server_key: "${NAKAMA_SERVER_KEY}"

session:
  encryption_key: "${NAKAMA_SESSION_KEY}"

runtime:
  js_entrypoint: "match.js"
